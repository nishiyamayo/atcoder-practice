package agc026

import java.util.Scanner

class B extends App {

  val sc = new Scanner(System.in)

  val t = sc.nextInt
  (1 to t).foreach {_ =>
    val (a, b, c, d) = (sc.nextLong, sc.nextLong, sc.nextLong, sc.nextLong)

  }
}
